apiVersion: v1
kind: Service
metadata:
  name: spring-java21-service
spec:
  type: NodePort
  selector:
    app: spring-java21-demo
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30000